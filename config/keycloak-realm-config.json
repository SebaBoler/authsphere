{
  "realm": "myrealm",
  "enabled": true,
  "duplicateEmailsAllowed": false,
  "accessCodeLifespan": 60,
  "accessCodeLifespanUserAction": 60,
  "passwordPolicy": "hashAlgorithm(argon2) and maxLength(256) and passwordHistory(10) and length(10) and specialChars(1) and upperCase(1) and lowerCase(1) and digits(1) and notUsername(undefined) and notEmail(undefined)",
  "users": [
    {
      "username": "testuser",
      "enabled": true,
      "emailVerified": true,
      "firstName": "Test",
      "lastName": "User",
      "email": "testuser@example.com",
      "credentials": [
        {
          "type": "password",
          "value": "Test!password1234",
          "temporary": false
        }
      ]
    }
  ]
}
